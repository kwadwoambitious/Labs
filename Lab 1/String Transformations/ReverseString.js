// Reverse a string
function reverse(str) {
  // Split string into an array of characters
  // Reverse the array
  // Join the characters back into a string
  return str.split("").reverse().join("");
}

// Test the function with "Amalitech"
console.log(reverse("Amalitech"));
