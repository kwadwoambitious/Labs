// Count the number of words in a string
function wordCount(str) {
  // Split the string by spaces and return the array length
  return str.split(" ").length;
}

// Log the result of the wordCount function
console.log(wordCount("I am a frontend developer(NSP) at Amalitech"));
