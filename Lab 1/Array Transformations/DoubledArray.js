// Function to double each element in an array
function double(arr) {
  // Map each element to its double
  return arr.map((element) => element * 2);
}

// Test the function with an array
console.log(double([1, 2, 3, 4, 5]));
